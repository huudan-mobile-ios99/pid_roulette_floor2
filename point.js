class Point {
    constructor(PointTransactionID, PreferredName, Number, GamingDate, LoyaltyPoints) {
        this.PointTransactionID = PointTransactionID, this.PreferredName = PreferredName, this.Number = Number, this.GamingDate = GamingDate, this.LoyaltyPoints = LoyaltyPoints
    }
}

module.exports = Point;